package com.halfacode.spring_real_time_learning.model;

public class UserContext {

    // For ThreadLocal example
    //static ThreadLocal<String> user = new ThreadLocal<>();

    // For ScopedValue example
  //  static ScopedValue<String> userScopedVal = ScopedValue.newInstance();
}

